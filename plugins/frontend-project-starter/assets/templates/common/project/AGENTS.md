## Agent Entry

Shared project instructions live in [`./.agents/AGENTS.md`](./.agents/AGENTS.md).

Before changing frontend code, read and follow that file. Codex-compatible skills are exposed through `./.codex/skills`, which should be a bridge or mirror of `./.agents/skills`.

If your agent runtime does not automatically follow links or includes, open `./.agents/AGENTS.md` directly before proceeding.
