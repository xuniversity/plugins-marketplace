export interface NoticeConfig {
  cancelText?: string;
  confirmText?: string;
  contactLines?: string[];
  description?: string;
  items: string[];
  qrCodeCaption?: string;
  qrCodeTitle?: string;
  qrCodeUrl?: string;
  title: string;
}
