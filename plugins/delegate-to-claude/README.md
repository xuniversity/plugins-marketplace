# delegate-to-claude

Codex-only 插件，包含 `delegate-to-claude` 单个 skill。

该插件用于在 Codex 中将明确任务委托给本机 Claude Code headless 执行，并通过统一的委托目录协议回收状态、结果与日志，再由 Codex 进行最终验收。

## Skill path

- `skills/delegate-to-claude/SKILL.md`
